required :
    - name   : value
      type   : int
      help   :
    - name   : mode
      type   : str
      values : ["precise", "approximate"]
      help   :

switches :
    - name   : debug
      handle : d
      help   : enable debug logging

flags :
    - name    : limit
      handle  : l
      type    : int
      default : 100
      help    :
