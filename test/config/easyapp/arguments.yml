required :
    - name : operations
      help : the operations to run

switches :
    - name   : debug
      handle : d
      help   : enable debug logging

flags :
    - name    : max
      handle  : m
      type    : int
      default : -1
      help    : maximum number of operations
